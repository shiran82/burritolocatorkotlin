<resources>

    <!-- Base application theme. -->
    <style name="AppTheme.Launcher" parent="android:Theme.Black.NoTitleBar">
        <item name="android:windowBackground">
            @drawable/ic_launcher
        </item>
    </style>

</resources>
